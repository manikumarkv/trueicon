#!/usr/bin/env node

// Placeholder entry point. The MCP server (stdio transport) will be implemented in a later phase.
// Log to stderr so stdout stays reserved for the MCP protocol stream.
console.error("trueicon: starting (placeholder, server not yet implemented)");
